// docker run -i -t --rm -v $(pwd):/work -w /work java:8-jdk bash
// OR
// docker run -i -t --rm -v $(pwd):/work -w /work codeclou/docker-oracle-jdk:8u131bash

// javac Test.java
// java -Dsun.java2d.debugfonts=true -cp . Test
